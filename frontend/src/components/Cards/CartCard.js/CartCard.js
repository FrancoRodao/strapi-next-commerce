import styled from 'styled-components'

const Container = styled.div`
  padding: 30px;
`

export function CartCard({ title, price, quantity }) {
  return <Container>esto es una tarjeta del carro</Container>
}
